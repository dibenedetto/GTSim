namespace GTSim
{
	public class Constants
	{
		public const float TIME_SCALE                = 10.0f;
		public const float KMH_TO_MS                 = (1.0f / 3.6f);
		public const float MAX_SPEED_FRICTION_FACTOR = 1.2f;
		public const float MAX_SPEED                 = 150.0f * (1.0f / 3.6f);
	}
}
